// src/setupTests.js
// Этот файл запускается перед каждым тестом
// Подключает дополнительные матчеры для тестирования DOM

import '@testing-library/jest-dom'

// Мок для CSS-модулей — Jest не умеет обрабатывать CSS
// Возвращаем пустой объект вместо стилей
jest.mock('./index.css', () => ({}))
